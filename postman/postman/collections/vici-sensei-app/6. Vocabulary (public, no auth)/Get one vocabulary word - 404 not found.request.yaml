$kind: http-request
url: "{{baseUrl}}/api/vocabulary/{{missingId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 5000
