$kind: http-request
url: "{{baseUrl}}/api/vocabulary/99999999999999999999"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('an id past the bigint range (int8 max is ~9.2e18) does not silently succeed with a 200', () => {
        pm.expect(pm.response.code).to.not.equal(200);
      });
    language: text/javascript
order: 4650
