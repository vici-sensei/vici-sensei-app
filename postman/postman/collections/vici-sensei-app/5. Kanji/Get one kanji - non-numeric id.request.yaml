$kind: http-request
url: "{{baseUrl}}/api/kanji/not-a-number"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('a non-numeric id fails the bigint cast at the DB level and surfaces as a 500 (documents a gap: the route should probably validate the id and return 400 instead)', () => {
        pm.response.to.have.status(500);
      });
    language: text/javascript
order: 4600
