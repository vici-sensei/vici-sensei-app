$kind: http-request
url: "{{baseUrl}}/api/kanji?level=n5"
method: GET
queryParams:
  level: n5
scripts:
  - type: afterResponse
    code: |-
      pm.test('level check is case-sensitive, so lowercase is rejected', () => {
        pm.response.to.have.status(400);
      });
    language: text/javascript
order: 3100
