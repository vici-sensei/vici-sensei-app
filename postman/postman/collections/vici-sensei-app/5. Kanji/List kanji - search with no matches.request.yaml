$kind: http-request
url: "{{baseUrl}}/api/kanji?search=zzznonexistentsearchterm123"
method: GET
queryParams:
  search: zzznonexistentsearchterm123
scripts:
  - type: afterResponse
    code: |-
      pm.test('status is 200', () => pm.response.to.have.status(200));
      const body = pm.response.json();
      pm.test('empty data and zero count', () => {
        pm.expect(body.data).to.have.lengthOf(0);
        pm.expect(body.count).to.equal(0);
      });
    language: text/javascript
order: 2100
